{
  "name": "cityzen",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "clean": "rm -rf shared/dist server/dist client/dist shared/tsconfig.tsbuildinfo server/tsconfig.tsbuildinfo",
    "build": "npm run clean && npm run build -w shared && npm run build -w server && npm run build -w client",
    "lint": "eslint . --ext .ts"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "prisma": "^7.4.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@prisma/client": "^7.4.0",
    "@supabase/supabase-js": "^2.95.3",
    "@xdevplatform/xdk": "^0.4.0"
  }
}
